import createProvider from '@ad/vue-hook-model';
export const {
    createContext,
    useContext,
} = createProvider(() => {

    return {
        state: {

        },
        reducers: {

        },
        effects: {

        }
    }
})